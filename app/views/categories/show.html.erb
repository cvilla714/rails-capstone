<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<p>
  <strong>Priority:</strong>
  <%= @category.priority %>
</p>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>


<ul>
  <li><% @articles.each do |art|%></li>
  <li><%= link_to art.title,article_path %></li>
  <li><%= art.author_id%></li>
  <% end %>
</ul>